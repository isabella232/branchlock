NAME := branchlock
VERSION := 1.0
DESCRIPTION := 'Branchlock:  Adds permission for a specific user to a specific branch and can remove branch restrictions'
DOCUMENTATION := 'https://github.com/xoom/branchlock'

include common.mk
